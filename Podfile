# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PaginationTask' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PaginationTask
  target 'PaginationTaskTests' do
    
  pod 'Alamofire', '~> 5.4.0'
  
    inherit! :search_paths
    # Pods for testing
  end

  target 'PaginationTaskUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
